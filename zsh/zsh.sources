# for Node Version Manager
source $(brew --prefix nvm)/nvm.sh
# If you are in various user mode, use this PS1
# export PS1="%{$fg_bold[red]%}($(whoami)) ${ret_status}%{$fg_bold[green]%}%p %{$fg[cyan]%}%c %{$fg_bold[blue]%}$(git_prompt_info)%{$fg_bold[blue]%} % %{$reset_color%}"
