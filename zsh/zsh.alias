# use sublime text to view these files

## comment below because these can excute via command
# alias -s js=st
# alias -s coffee=st
# alias -s py=st
# alias -s rb=st
# alias -s sh=st

alias -s html=st
alias -s md=st
alias -s css=st
alias -s c=st
alias -s cpp=st
alias -s h=st
alias -s hpp=st
alias -s php=st

# use less to view log files
alias -s log=less

alias -g PR=http_proxy=http://127.0.0.1:8087
# PR curl https://twitter.com # 相当于 http_proxy=http://127.0.0.1:8087 curl https://twitter.com